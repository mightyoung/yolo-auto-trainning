# Training API
# Contains: YOLO Training, HPO, Model Export, Auto Label
# Target: GPU Server (Requires CUDA)

# Core
ultralytics>=8.0.0
ray[tune]>=2.0.0

# Auto Label dependencies
# GroundingDINO
transformers>=4.35.0
torch>=2.0.0
accelerate>=0.25.0

# SAM (Segment Anything)
# Note: Install manually on GPU server if needed
# pip install segment-anything

# Supervision (for detection utilities)
supervision>=0.20.0

# API
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Task Queue
celery>=5.3.0
redis>=5.0.0

# Authentication
PyJWT>=2.8.0
python-jose[cryptography]>=3.3.0

# Utilities
python-dotenv>=1.0.0
opencv-python>=4.8.0
pillow>=10.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
